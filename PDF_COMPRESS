<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">

<title>PDF Compressor - Target KB / MB</title>

<style>
*{
    margin:0;
    padding:0;
    box-sizing:border-box;
    font-family:Arial,Helvetica,sans-serif;
}

:root{
    --glass:rgba(255,255,255,.075);
    --border:rgba(255,255,255,.16);
    --text:#ffffff;
    --muted:#aeb9d5;
    --cyan:#00eaff;
    --blue:#168cff;
    --pink:#ff38c7;
    --green:#00e6a7;
    --red:#ff416c;
}

body{
    min-height:100vh;
    color:var(--text);
    background:
        radial-gradient(circle at 10% 10%,rgba(0,234,255,.16),transparent 30%),
        radial-gradient(circle at 90% 10%,rgba(255,56,199,.15),transparent 30%),
        radial-gradient(circle at 50% 100%,rgba(22,140,255,.18),transparent 35%),
        linear-gradient(135deg,#050817,#0b1024 50%,#050714);
    overflow-x:hidden;
}

/* Background glowing orbs */
.orb{
    position:fixed;
    border-radius:50%;
    pointer-events:none;
    filter:blur(80px);
    opacity:.35;
    z-index:0;
}

.orb1{
    width:280px;
    height:280px;
    background:#00eaff;
    top:-100px;
    left:-100px;
    animation:orbMove1 10s infinite alternate ease-in-out;
}

.orb2{
    width:300px;
    height:300px;
    background:#ff00b7;
    right:-120px;
    top:20%;
    animation:orbMove2 12s infinite alternate ease-in-out;
}

.orb3{
    width:280px;
    height:280px;
    background:#176cff;
    bottom:-120px;
    left:35%;
    animation:orbMove3 14s infinite alternate ease-in-out;
}

@keyframes orbMove1{
    to{transform:translate(150px,100px);}
}

@keyframes orbMove2{
    to{transform:translate(-120px,120px);}
}

@keyframes orbMove3{
    to{transform:translate(100px,-100px);}
}

.container{
    position:relative;
    z-index:2;
    width:min(1150px,94%);
    margin:35px auto;
}

/* HEADER */

.header{
    position:relative;
    overflow:hidden;
    text-align:center;
    padding:32px 20px;
    border:1px solid var(--border);
    border-radius:30px;
    background:rgba(255,255,255,.07);
    backdrop-filter:blur(22px);
    box-shadow:
        0 25px 70px rgba(0,0,0,.35),
        inset 0 1px 0 rgba(255,255,255,.15);
}

.header:after{
    content:"";
    position:absolute;
    top:0;
    left:-120%;
    width:55%;
    height:100%;
    background:linear-gradient(
        90deg,
        transparent,
        rgba(255,255,255,.25),
        transparent
    );
    transform:skewX(-20deg);
    animation:shine 4.5s infinite;
}

@keyframes shine{
    0%{left:-120%;}
    50%,100%{left:150%;}
}

/* PDF LOGO */

.pdf-logo{
    width:90px;
    height:105px;
    margin:auto;
    display:flex;
    align-items:center;
    justify-content:center;
    position:relative;
    border-radius:17px;
    background:linear-gradient(145deg,#d71920,#ff1744);
    box-shadow:
        0 0 12px rgba(255,23,68,.8),
        0 0 35px rgba(255,23,68,.45),
        inset 0 2px 5px rgba(255,255,255,.35);
    font-size:22px;
    font-weight:900;
    letter-spacing:2px;
}

.pdf-logo:before{
    content:"";
    position:absolute;
    top:0;
    right:0;
    width:29px;
    height:29px;
    background:#ff8a80;
    clip-path:polygon(0 0,100% 100%,100% 0);
}

.pdf-logo:after{
    content:"";
    position:absolute;
    bottom:-8px;
    width:60px;
    height:5px;
    border-radius:50%;
    background:#ff1744;
    filter:blur(7px);
}

.title{
    margin-top:20px;
    font-size:clamp(30px,5vw,50px);
    font-weight:900;
    letter-spacing:2px;
    background:linear-gradient(
        90deg,
        #fff,
        #66ffff,
        #fff,
        #ff8bea,
        #fff
    );
    background-size:300%;
    -webkit-background-clip:text;
    background-clip:text;
    color:transparent;
    animation:titleShine 5s linear infinite;
}

@keyframes titleShine{
    to{background-position:300%;}
}

.subtitle{
    margin-top:9px;
    color:var(--muted);
    font-size:15px;
}

/* MAIN */

.panel{
    margin-top:24px;
    padding:25px;
    border-radius:28px;
    background:var(--glass);
    border:1px solid var(--border);
    backdrop-filter:blur(20px);
    box-shadow:
        0 25px 70px rgba(0,0,0,.32),
        inset 0 1px 0 rgba(255,255,255,.1);
}

/* UPLOAD */

.drop-zone{
    position:relative;
    overflow:hidden;
    display:block;
    text-align:center;
    padding:48px 20px;
    border:2px dashed rgba(0,234,255,.55);
    border-radius:23px;
    background:rgba(0,220,255,.035);
    cursor:pointer;
    transition:.3s ease;
}

.drop-zone:hover,
.drop-zone.dragover{
    transform:translateY(-3px);
    border-color:#66ffff;
    background:rgba(0,234,255,.08);
    box-shadow:
        0 0 30px rgba(0,234,255,.18),
        inset 0 0 35px rgba(0,234,255,.04);
}

.upload-icon{
    font-size:62px;
    line-height:1;
    margin-bottom:16px;
    filter:drop-shadow(0 0 15px rgba(255,50,70,.65));
}

.drop-title{
    font-size:23px;
    font-weight:800;
}

.drop-text{
    color:var(--muted);
    margin-top:9px;
    font-size:14px;
}

#fileInput{
    display:none;
}

/* TARGET */

.target-box{
    margin-top:22px;
    padding:22px;
    border-radius:21px;
    background:rgba(0,0,0,.16);
    border:1px solid rgba(255,255,255,.11);
}

.target-label{
    display:flex;
    align-items:center;
    gap:9px;
    margin-bottom:12px;
    font-size:17px;
    font-weight:800;
}

.target-row{
    display:flex;
    gap:11px;
}

.target-input{
    flex:1;
    min-width:0;
    padding:15px 17px;
    border-radius:14px;
    border:1px solid rgba(255,255,255,.16);
    outline:none;
    background:#10172d;
    color:#fff;
    font-size:17px;
    font-weight:bold;
}

.target-unit{
    width:105px;
    padding:15px 10px;
    border-radius:14px;
    border:1px solid rgba(255,255,255,.16);
    outline:none;
    background:#10172d;
    color:#fff;
    font-size:16px;
    font-weight:bold;
}

.target-input:focus,
.target-unit:focus{
    border-color:var(--cyan);
    box-shadow:
        0 0 16px rgba(0,234,255,.20),
        inset 0 0 10px rgba(0,234,255,.03);
}

.target-help{
    color:#8997b8;
    font-size:12px;
    margin-top:10px;
}

/* BUTTONS */

.buttons{
    display:flex;
    gap:12px;
    flex-wrap:wrap;
    margin-top:22px;
}

.btn{
    position:relative;
    overflow:hidden;
    border:0;
    border-radius:14px;
    padding:15px 24px;
    color:#fff;
    font-size:15px;
    font-weight:800;
    cursor:pointer;
    transition:.25s;
}

.btn:before{
    content:"";
    position:absolute;
    top:0;
    left:-110%;
    width:60%;
    height:100%;
    transform:skewX(-20deg);
    background:linear-gradient(
        90deg,
        transparent,
        rgba(255,255,255,.3),
        transparent
    );
}

.btn:hover:before{
    animation:buttonShine .7s;
}

@keyframes buttonShine{
    to{left:140%;}
}

.btn:hover{
    transform:translateY(-3px);
}

.btn:active{
    transform:scale(.98);
}

.compress{
    background:linear-gradient(135deg,#008cff,#00eaff);
    box-shadow:0 0 22px rgba(0,234,255,.3);
}

.download{
    background:linear-gradient(135deg,#00b894,#00e6a7);
    box-shadow:0 0 20px rgba(0,230,167,.25);
}

.clear{
    background:linear-gradient(135deg,#ff416c,#ff4b2b);
    box-shadow:0 0 20px rgba(255,65,108,.25);
}

.btn:disabled{
    opacity:.4;
    cursor:not-allowed;
    transform:none;
}

/* PROGRESS */

.progress-box{
    display:none;
    margin-top:22px;
    padding:17px;
    border-radius:17px;
    background:rgba(0,0,0,.15);
    border:1px solid rgba(255,255,255,.1);
}

.progress-info{
    display:flex;
    justify-content:space-between;
    color:#c8d2eb;
    font-size:13px;
    margin-bottom:9px;
}

.progress-track{
    width:100%;
    height:13px;
    overflow:hidden;
    border-radius:20px;
    background:rgba(255,255,255,.08);
}

.progress-bar{
    width:0;
    height:100%;
    border-radius:20px;
    background:linear-gradient(
        90deg,
        #00eaff,
        #487cff,
        #ff39d1
    );
    box-shadow:0 0 17px rgba(0,234,255,.7);
    transition:width .25s;
}

/* FILE GRID */

.files{
    display:grid;
    grid-template-columns:
        repeat(auto-fit,minmax(300px,1fr));
    gap:16px;
    margin-top:24px;
}

.file-card{
    position:relative;
    overflow:hidden;
    padding:18px;
    border-radius:20px;
    background:rgba(255,255,255,.055);
    border:1px solid rgba(255,255,255,.11);
    box-shadow:0 12px 35px rgba(0,0,0,.2);
    animation:cardIn .4s ease;
}

.file-card:before{
    content:"";
    position:absolute;
    top:-60px;
    right:-60px;
    width:130px;
    height:130px;
    border-radius:50%;
    background:rgba(0,234,255,.06);
    filter:blur(25px);
}

@keyframes cardIn{
    from{
        opacity:0;
        transform:translateY(15px);
    }
    to{
        opacity:1;
        transform:translateY(0);
    }
}

.file-top{
    display:flex;
    gap:13px;
    align-items:center;
}

.mini-pdf{
    flex-shrink:0;
    width:50px;
    height:60px;
    display:flex;
    align-items:center;
    justify-content:center;
    border-radius:9px;
    background:linear-gradient(145deg,#d71920,#ff3b4d);
    box-shadow:0 0 15px rgba(255,30,60,.35);
    font-size:12px;
    font-weight:900;
}

.file-info{
    min-width:0;
}

.file-name{
    font-weight:800;
    white-space:nowrap;
    overflow:hidden;
    text-overflow:ellipsis;
}

.file-size{
    margin-top:5px;
    color:#94a1c0;
    font-size:12px;
}

.status{
    margin-top:15px;
    padding-top:13px;
    border-top:1px solid rgba(255,255,255,.08);
    color:#b9c4df;
    font-size:13px;
    line-height:1.6;
}

.result-line{
    margin-top:7px;
    font-weight:800;
}

.saved{
    color:#54ffc7;
}

.not-smaller{
    color:#ffc46b;
}

.card-download{
    width:100%;
    margin-top:13px;
    padding:11px;
    border:0;
    border-radius:11px;
    color:white;
    background:rgba(0,234,255,.1);
    border:1px solid rgba(0,234,255,.2);
    cursor:pointer;
    font-weight:bold;
}

.card-download:hover{
    background:rgba(0,234,255,.18);
}

/* INFO */

.info{
    margin-top:22px;
    padding:18px;
    border-radius:18px;
    background:rgba(0,234,255,.035);
    border:1px solid rgba(0,234,255,.11);
    color:#aebbd8;
    font-size:13px;
    line-height:1.7;
}

.info strong{
    color:#65ffff;
}

/* FOOTER */

.footer{
    text-align:center;
    color:#697796;
    font-size:12px;
    margin:22px 0 35px;
}

/* RESPONSIVE */

@media(max-width:650px){

    .container{
        width:94%;
        margin:18px auto;
    }

    .panel{
        padding:15px;
    }

    .target-row{
        flex-direction:column;
    }

    .target-unit{
        width:100%;
    }

    .buttons{
        flex-direction:column;
    }

    .btn{
        width:100%;
    }

    .drop-zone{
        padding:38px 15px;
    }

}
</style>
</head>

<body>

<div class="orb orb1"></div>
<div class="orb orb2"></div>
<div class="orb orb3"></div>

<div class="container">

    <!-- HEADER -->

    <div class="header">

        <div class="pdf-logo">
            PDF
        </div>

        <div class="title">
            PDF COMPRESSOR ✨
        </div>

        <div class="subtitle">
            Compress your PDF to your expected KB or MB size
        </div>

    </div>


    <!-- MAIN -->

    <div class="panel">

        <!-- UPLOAD -->

        <label class="drop-zone" id="dropZone">

            <div class="upload-icon">
                📕
            </div>

            <div class="drop-title">
                Select PDF Files
            </div>

            <div class="drop-text">
                Click here or drag & drop PDF files
            </div>

            <div class="drop-text">
                Multiple PDF files supported
            </div>

            <input
                type="file"
                id="fileInput"
                accept=".pdf,application/pdf"
                multiple
            >

        </label>


        <!-- TARGET SIZE -->

        <div class="target-box">

            <div class="target-label">
                🎯 Expected PDF Size
            </div>

            <div class="target-row">

                <input
                    class="target-input"
                    id="targetSize"
                    type="number"
                    min="1"
                    step="1"
                    value="500"
                    placeholder="Enter expected size"
                >

                <select
                    class="target-unit"
                    id="targetUnit"
                >
                    <option value="KB">KB</option>
                    <option value="MB">MB</option>
                </select>

            </div>

            <div class="target-help">
                Example: enter <b>500 KB</b> or <b>2 MB</b>.
                The compressor will try multiple quality levels
                to reach the selected target.
            </div>

        </div>


        <!-- BUTTONS -->

        <div class="buttons">

            <button
                class="btn compress"
                id="compressBtn"
                disabled
            >
                ⚡ COMPRESS PDF
            </button>

            <button
                class="btn download"
                id="downloadAllBtn"
                disabled
            >
                📥 DOWNLOAD ALL
            </button>

            <button
                class="btn clear"
                id="clearBtn"
            >
                🗑️ CLEAR ALL
            </button>

        </div>


        <!-- PROGRESS -->

        <div
            class="progress-box"
            id="progressBox"
        >

            <div class="progress-info">

                <span id="progressText">
                    Preparing...
                </span>

                <span id="progressPercent">
                    0%
                </span>

            </div>

            <div class="progress-track">

                <div
                    class="progress-bar"
                    id="progressBar"
                ></div>

            </div>

        </div>


        <!-- FILES -->

        <div
            class="files"
            id="files"
        ></div>


        <!-- INFORMATION -->

        <div class="info">

            <strong>✨ Smart Target Compression</strong><br>

            The compressor automatically tries different image
            qualities and page scales to get the output close to
            your selected KB/MB target.

            <br><br>

            <strong>🔒 Privacy:</strong>
            PDF processing is performed inside your browser.
            Your files are not intentionally uploaded to a server.

            <br><br>

            <strong>⚠️ Note:</strong>
            Exact target size cannot always be guaranteed.
            PDF files containing mostly text/vector content may
            have limited size reduction. Image/scanned PDFs
            generally offer more compression potential.

        </div>

    </div>


    <div class="footer">
        ✨ PDF Compressor • Target KB / MB • Browser Based
    </div>

</div>


<!-- PDF.JS -->

<script
    src="https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.min.mjs"
    type="module">
</script>


<!-- JSPDF -->

<script
    src="https://cdnjs.cloudflare.com/ajax/libs/jspdf/2.5.1/jspdf.umd.min.js">
</script>


<script type="module">

import * as pdfjsLib
from "https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.min.mjs";


pdfjsLib.GlobalWorkerOptions.workerSrc =
"https://cdnjs.cloudflare.com/ajax/libs/pdf.js/4.10.38/pdf.worker.min.mjs";


const fileInput =
document.getElementById("fileInput");

const dropZone =
document.getElementById("dropZone");

const targetSize =
document.getElementById("targetSize");

const targetUnit =
document.getElementById("targetUnit");

const compressBtn =
document.getElementById("compressBtn");

const downloadAllBtn =
document.getElementById("downloadAllBtn");

const clearBtn =
document.getElementById("clearBtn");

const filesContainer =
document.getElementById("files");

const progressBox =
document.getElementById("progressBox");

const progressBar =
document.getElementById("progressBar");

const progressText =
document.getElementById("progressText");

const progressPercent =
document.getElementById("progressPercent");


let selectedFiles = [];
let compressedFiles = [];


/* --------------------------------
   FILE SELECTION
-------------------------------- */

fileInput.addEventListener(
    "change",
    () => {

        addFiles(
            Array.from(fileInput.files)
        );

    }
);


/* --------------------------------
   DRAG & DROP
-------------------------------- */

["dragenter","dragover"].forEach(
    eventName => {

        dropZone.addEventListener(
            eventName,
            e => {

                e.preventDefault();

                dropZone.classList.add(
                    "dragover"
                );

            }
        );

    }
);


["dragleave","drop"].forEach(
    eventName => {

        dropZone.addEventListener(
            eventName,
            e => {

                e.preventDefault();

                dropZone.classList.remove(
                    "dragover"
                );

            }
        );

    }
);


dropZone.addEventListener(
    "drop",
    e => {

        const files =
            Array.from(
                e.dataTransfer.files
            ).filter(
                file =>
                    file.type ===
                    "application/pdf" ||
                    file.name
                        .toLowerCase()
                        .endsWith(".pdf")
            );

        addFiles(files);

    }
);


/* --------------------------------
   ADD FILES
-------------------------------- */

function addFiles(files){

    const pdfs =
        files.filter(
            file =>
                file.type ===
                "application/pdf" ||
                file.name
                    .toLowerCase()
                    .endsWith(".pdf")
        );

    selectedFiles.push(...pdfs);

    renderFiles();

    compressBtn.disabled =
        selectedFiles.length === 0;

    downloadAllBtn.disabled = true;

    compressedFiles = [];

}


/* --------------------------------
   RENDER FILES
-------------------------------- */

function renderFiles(){

    filesContainer.innerHTML = "";

    selectedFiles.forEach(
        (file,index) => {

            const card =
                document.createElement("div");

            card.className =
                "file-card";

            card.innerHTML = `

                <div class="file-top">

                    <div class="mini-pdf">
                        PDF
                    </div>

                    <div class="file-info">

                        <div
                            class="file-name"
                            title="${escapeHTML(file.name)}"
                        >
                            ${escapeHTML(file.name)}
                        </div>

                        <div class="file-size">
                            Original:
                            ${formatBytes(file.size)}
                        </div>

                    </div>

                </div>

                <div
                    class="status"
                    id="status-${index}"
                >
                    ⏳ Ready to compress
                </div>

            `;

            filesContainer.appendChild(card);

        }
    );

}


/* --------------------------------
   ESCAPE HTML
-------------------------------- */

function escapeHTML(value){

    return value.replace(
        /[&<>"']/g,
        char => ({

            "&":"&amp;",
            "<":"&lt;",
            ">":"&gt;",
            '"':"&quot;",
            "'":"&#039;"

        })[char]
    );

}


/* --------------------------------
   FORMAT BYTES
-------------------------------- */

function formatBytes(bytes){

    if(bytes < 1024)
        return bytes + " B";

    if(bytes < 1024 * 1024)
        return (
            bytes / 1024
        ).toFixed(1) + " KB";

    return (
        bytes /
        (1024 * 1024)
    ).toFixed(2) + " MB";

}


/* --------------------------------
   TARGET BYTES
-------------------------------- */

function getTargetBytes(){

    const value =
        parseFloat(targetSize.value);

    if(!value || value <= 0)
        return null;

    if(targetUnit.value === "MB"){

        return Math.round(
            value *
            1024 *
            1024
        );

    }

    return Math.round(
        value * 1024
    );

}


/* --------------------------------
   COMPRESS ALL
-------------------------------- */

compressBtn.addEventListener(
    "click",
    async () => {

        const targetBytes =
            getTargetBytes();

        if(!targetBytes){

            alert(
                "Please enter a valid expected size."
            );

            return;

        }

        if(selectedFiles.length === 0)
            return;


        compressedFiles = [];

        compressBtn.disabled = true;

        downloadAllBtn.disabled = true;

        progressBox.style.display =
            "block";


        for(
            let i = 0;
            i < selectedFiles.length;
            i++
        ){

            const percent =
                Math.round(
                    (i /
                    selectedFiles.length) *
                    100
                );

            setProgress(
                percent,
                `Compressing ${i+1} of ${selectedFiles.length}`
            );


            const result =
                await compressPDF(
                    selectedFiles[i],
                    i,
                    targetBytes
                );


            compressedFiles.push(result);


            setProgress(
                Math.round(
                    ((i + 1) /
                    selectedFiles.length) *
                    100
                ),
                `Completed ${i+1} of ${selectedFiles.length}`
            );

        }


        progressText.textContent =
            "✨ Compression completed";

        setProgress(100);

        compressBtn.disabled = false;

        downloadAllBtn.disabled =
            compressedFiles.length === 0;

    }
);


/* --------------------------------
   PDF COMPRESSION
-------------------------------- */

async function compressPDF(
    file,
    index,
    targetBytes
){

    const status =
        document.getElementById(
            "status-" + index
        );


    status.innerHTML =
        "📖 Reading PDF...";


    const buffer =
        await file.arrayBuffer();


    const pdf =
        await pdfjsLib
            .getDocument({
                data:buffer
            })
            .promise;


    /*
        Try several quality/scale combinations.
        Highest quality is tried first.
    */

    const attempts = [

        {scale:1.00,quality:.90},
        {scale:.95,quality:.82},
        {scale:.90,quality:.75},
        {scale:.85,quality:.68},
        {scale:.80,quality:.60},
        {scale:.75,quality:.52},
        {scale:.70,quality:.45},
        {scale:.65,quality:.38},
        {scale:.60,quality:.32},
        {scale:.55,quality:.26},
        {scale:.50,quality:.20}

    ];


    let bestBlob = null;

    let bestSize = Infinity;


    for(
        let attempt = 0;
        attempt < attempts.length;
        attempt++
    ){

        const setting =
            attempts[attempt];


        status.innerHTML =
            `⚙️ Compressing:
             ${attempt + 1}/${attempts.length}
             • ${pdf.numPages} pages`;


        const blob =
            await buildPDF(
                pdf,
                setting.scale,
                setting.quality
            );


        const size =
            blob.size;


        /*
            Keep the smallest result found.
        */

        if(size < bestSize){

            bestSize = size;

            bestBlob = blob;

        }


        /*
            Stop when target is reached.
        */

        if(size <= targetBytes){

            break;

        }


        /*
            Yield to browser.
        */

        await sleep(20);

    }


    const originalSize =
        file.size;


    const newSize =
        bestBlob.size;


    const saving =
        (1 -
        newSize /
        originalSize) *
        100;


    const outputName =
        file.name.replace(
            /\.pdf$/i,
            ""
        ) +
        "_compressed.pdf";


    if(newSize <= targetBytes){

        status.innerHTML = `

            <div>
                ✅ Target reached
            </div>

            <div class="result-line">
                ${formatBytes(originalSize)}
                →
                ${formatBytes(newSize)}
            </div>

            <div class="result-line saved">
                🎉 Saved ${Math.max(
                    0,
                    saving
                ).toFixed(1)}%
            </div>

        `;

    }else{

        status.innerHTML = `

            <div>
                ⚠️ Smallest available result
            </div>

            <div class="result-line">
                ${formatBytes(originalSize)}
                →
                ${formatBytes(newSize)}
            </div>

            <div class="result-line not-smaller">
                Target:
                ${formatBytes(targetBytes)}
            </div>

            <div class="result-line saved">
                💾 Saved ${Math.max(
                    0,
                    saving
                ).toFixed(1)}%
            </div>

        `;

    }


    addDownloadButton(
        index,
        bestBlob,
        outputName
    );


    return {

        name:outputName,
        blob:bestBlob,
        originalSize:originalSize,
        compressedSize:newSize

    };

}


/* --------------------------------
   BUILD PDF
-------------------------------- */

async function buildPDF(
    pdf,
    scale,
    quality
){

    const {
        jsPDF
    } = window.jspdf;


    let output = null;


    for(
        let pageNumber = 1;
        pageNumber <= pdf.numPages;
        pageNumber++
    ){

        const page =
            await pdf.getPage(
                pageNumber
            );


        const viewport =
            page.getViewport({
                scale:scale
            });


        const canvas =
            document.createElement(
                "canvas"
            );


        const context =
            canvas.getContext(
                "2d",
                {
                    alpha:false
                }
            );


        canvas.width =
            Math.max(
                1,
                Math.round(
                    viewport.width
                )
            );


        canvas.height =
            Math.max(
                1,
                Math.round(
                    viewport.height
                )
            );


        /*
            White background
        */

        context.fillStyle =
            "#ffffff";

        context.fillRect(
            0,
            0,
            canvas.width,
            canvas.height
        );


        await page.render({

            canvasContext:context,

            viewport:viewport

        }).promise;


        const image =
            canvas.toDataURL(
                "image/jpeg",
                quality
            );


        const orientation =
            viewport.width >
            viewport.height
            ? "landscape"
            : "portrait";


        if(!output){

            output =
                new jsPDF({

                    orientation:
                        orientation,

                    unit:"pt",

                    format:[
                        viewport.width,
                        viewport.height
                    ],

                    compress:true

                });

        }else{

            output.addPage(
                [
                    viewport.width,
                    viewport.height
                ],
                orientation
            );

        }


        output.addImage(
            image,
            "JPEG",
            0,
            0,
            viewport.width,
            viewport.height,
            undefined,
            "FAST"
        );


        /*
            Release canvas memory
        */

        canvas.width = 1;
        canvas.height = 1;

    }


    return output.output("blob");

}


/* --------------------------------
   ADD DOWNLOAD BUTTON
-------------------------------- */

function addDownloadButton(
    index,
    blob,
    filename
){

    const card =
        filesContainer.children[index];


    if(!card)
        return;


    const oldButton =
        card.querySelector(
            ".card-download"
        );


    if(oldButton)
        oldButton.remove();


    const button =
        document.createElement(
            "button"
        );


    button.className =
        "card-download";


    button.innerHTML =
        "📥 DOWNLOAD COMPRESSED PDF";


    button.addEventListener(
        "click",
        () => {

            downloadBlob(
                blob,
                filename
            );

        }
    );


    card.appendChild(button);

}


/* --------------------------------
   DOWNLOAD
-------------------------------- */

function downloadBlob(
    blob,
    filename
){

    const url =
        URL.createObjectURL(blob);


    const link =
        document.createElement("a");


    link.href = url;

    link.download = filename;


    document.body.appendChild(link);

    link.click();

    link.remove();


    setTimeout(
        () => URL.revokeObjectURL(url),
        1500
    );

}


/* --------------------------------
   DOWNLOAD ALL
-------------------------------- */

downloadAllBtn.addEventListener(
    "click",
    async () => {

        if(!compressedFiles.length)
            return;


        for(
            const item of compressedFiles
        ){

            downloadBlob(
                item.blob,
                item.name
            );


            await sleep(500);

        }

    }
);


/* --------------------------------
   CLEAR ALL
-------------------------------- */

clearBtn.addEventListener(
    "click",
    () => {

        selectedFiles = [];

        compressedFiles = [];

        fileInput.value = "";

        filesContainer.innerHTML = "";

        progressBox.style.display =
            "none";

        compressBtn.disabled = true;

        downloadAllBtn.disabled = true;

        setProgress(
            0,
            "Preparing..."
        );

    }
);


/* --------------------------------
   PROGRESS
-------------------------------- */

function setProgress(
    value,
    text
){

    value =
        Math.max(
            0,
            Math.min(
                100,
                value
            )
        );


    progressBar.style.width =
        value + "%";


    progressPercent.textContent =
        value + "%";


    if(text)
        progressText.textContent =
            text;

}


/* --------------------------------
   SLEEP
-------------------------------- */

function sleep(ms){

    return new Promise(
        resolve =>
            setTimeout(
                resolve,
                ms
            )
    );

}

</script>

</body>
</html>
